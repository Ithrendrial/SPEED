{
  "version": 2,
  "builds": [
    {
      "src": "src/main.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "backend/main.ts" },
    { "src": "/(.*)", "dest": "frontend/.next/$1" }
  ]
}
